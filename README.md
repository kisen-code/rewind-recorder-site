# 巻き戻し録音 — 公開ページ

App Store 提出に必要なプライバシーポリシーを置くためだけのリポジトリ。

⚠️ **`index.html` を直接編集しない。**本文は非公開リポジトリ `rewind-recorder` の
`docs/privacy-policy.md` が正で、アプリ内表示も同じファイルを使っている。
更新するときは向こうで本文を直し、`Tools/build-privacy-page.py` で作り直して差し替える。
